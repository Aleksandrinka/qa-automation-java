package com.tcs.edu.enums;

/**
 * Класс для хранения параметров дублирования сообщений
 * DOUBLES:  разрешено дублирование
 * DISTINCT: без дублирования
 *
 * @author a.chvarova
 */

public enum Doubling {
    DOUBLES,
    DISTINCT
}
